name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  schedule:
    - cron: '31 2 * * 3'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
      - run: bash -n deploy/vps/memora-update.sh && bash deploy/vps/test-memora-update.sh

  wallet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: python3 -m pip install -r services/memora-wallet-manager/requirements.txt pip-audit
      - run: python3 -m pip_audit -r services/memora-wallet-manager/requirements.txt
      - run: python3 services/memora-wallet-manager/check_runtime.py
      - run: python3 -m unittest discover -s services/memora-wallet-manager/tests -v
      - run: python3 -m py_compile services/memora-wallet-manager/bot.py services/memora-wallet-manager/storage.py

  site:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm audit --audit-level=high
      - run: node --check server.js && for file in server/*.js; do node --check "$file"; done
      - run: npm run test:deals
      - run: npm run test:payments
      - run: npm run test:reliability
      - run: npm run lint
      - run: npm run build

  pomodoro:
    runs-on: ubuntu-latest
    env:
      MEMORA_SCENE_KEY: ${{ secrets.MEMORA_SCENE_KEY }}
    defaults:
      run:
        working-directory: memora-pomodoro
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: memora-pomodoro/package-lock.json

      - run: npm ci
      - run: npm audit --audit-level=high
      - run: npx tsc --noEmit
      - name: Build public desktop and web without credentials
        run: npm run build:public && npm run build:web:public
      - run: npm run build
        if: env.MEMORA_SCENE_KEY != ''
      - run: npm run build:web
        if: env.MEMORA_SCENE_KEY != ''

  codeql:
    name: codeql (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
      packages: read
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript, python]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Initialize CodeQL
        uses: github/codeql-action/init@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
        with:
          languages: ${{ matrix.language }}
      - name: Analyze
        uses: github/codeql-action/analyze@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
